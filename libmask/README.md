# libmask

> [!WARNING]
> Just like `mask-hx`, `libmask` is no longer maintained in favor of
> [`libver`](https://crates.io/crates/libver/), which has better structure and
> more features.

`libmask` is the library component of
[`mask-hx`](https://github.com/r6915ee/mask-hx). See the documentation for
more information.
